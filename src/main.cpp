#include <iostream>
#include "Game.hpp"
#include "Pin.hpp"
#include "Pins.hpp"

// void GetRandomPins(Pins & pins)
// {
//     int random {};
   
//     for (auto i = 0; i < 4; i++)
//     {
//         random = rand() % 4;
//         pins.AddPin(testCombination1.at(random));
//     }
// }

int main ()
{
    srand(time(NULL));
    Game game;
    Pins pinstosolve;
    Pins playerpins;

    return 0;
}